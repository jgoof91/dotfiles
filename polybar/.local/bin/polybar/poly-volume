#!/bin/sh
toggle="$(amixer sget HDMI | grep -oE '\[(on|off)\]')"

if [ "${toggle}" = "[off]" ]; then
    printf "🔇 muted\n"
else
    printf "%s/100\n" "$(amixer sget HDMI | perl -ne 'print $1 if /(\d*)%/')"
fi
