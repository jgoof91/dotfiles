#!/bin/sh
toggle="$(amixer sget PCM | grep -oE '\[(on|off)\]')"

if [ "${toggle}" = "[off]" ]; then
    printf "🔇 muted"
else
    printf "%s/100%%" "$(amixer sget PCM | perl -ne 'print $1 if /(\d*)%/')"
fi
